{
  "name": "kinetic-particle-typography",
  "private": true,
  "version": "1.0.0",
  "description": "Canvas 2D metin rasterinden alfa kanaliyla parcacik hedefi cikarma, katmanli ornekleme, Morton sirasiyla esleme ve tek uniform'la surulen WebGL2 instanced morph",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "bench": "vite-node tools/bench.ts",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "prettier": "^3.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vite-node": "^2.1.0",
    "vitest": "^2.1.0"
  }
}
